const Discord = require('discord.js');
const fs = require('fs');

module.exports.run = async (bot, message, args) => {

    {
{
            message.delete();
        var pd = new Discord.RichEmbed()
        .setAuthor(`Sale gay de merde.`)
        .setColor('#03355b')
        .setImage('https://image.noelshack.com/fichiers/2019/21/5/1558650388-giphy.gif');
        message.channel.send(pd)
        } 
    }
}

module.exports.help = {
    name: "pd"
}
